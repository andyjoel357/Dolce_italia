import { StyleSheet, View, ViewBase } from "react-native";

export const styles = StyleSheet.create({
  image: {
    resizeMode: "contain",
    height: 200,
    width: "100%",
    marginTop: 20,
  },
  content: {
    marginHorizontal: 40,
    
  },
});
